---
name: Bug report
about: Something's not working as documented
labels: bug
---

### What happened

<!-- The observed behavior. Include the exact command and the output. Sanitize any access tokens. -->

### What you expected

<!-- Per the docs (link to the README / SETUP.md section if applicable). -->

### Reproduction

```sh
# commands you ran
```

### Environment

- ig-agent version: `ig-agent --version`
- OS + version:
- Node version (if you installed the bundled CLI): `node --version`
- Bun version (if you're running from source): `bun --version`

### `ig-agent auth status --pretty` output

<!-- The whole JSON is fine; no token fields are ever printed. Helps diagnose expiry / multi-account state. -->

```json
```

### Extra context

<!-- Anything else — error messages, stack traces (redact tokens), screenshots of Meta dev console if relevant, etc. -->
